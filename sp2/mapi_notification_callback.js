document.addEventListener("DOMContentLoaded", (event) => {
  console.log("DOMContentLoaded");

  try {
    // TODO: Get POST data and parse out... 
  }
  catch (e) {
    console.error("Failed to parse callback data into JSON:", e.message);
  }
});
